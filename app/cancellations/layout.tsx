import type { Metadata } from "next";

export const metadata: Metadata = {
  title: "Cancellations & Refunds Policy",
  description: "Understand the terms of booking cancellations, returns of artisan handicrafts, and refund rules on Island Connects Kiribati.",
};

export default function CancellationsLayout({ children }: { children: React.ReactNode }) {
  return children;
}
